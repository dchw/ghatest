name: GitHub Actions Demo
run-name: ${{ github.actor }} is testing out GitHub Actions 🚀
on: [push]
jobs:
  Job-hosted:
    runs-on: ubuntu-latest
    steps:
      - name: pstree
        run: |
          pstree -s -l -p $$
      - name: list?
        run: |
          pwd && ls -la
      - name: ls runner dir
        run: |
            ls -la /home/runner
      - name: env
        run: |
          env
      - name: tree
        run: |
          tree /home/runner
      - name: im gonna wreck it
        run: |
          echo "I'm gonna wreck it!" && mv /home/runner/runners/2.320.0/bin/Runner.Worker /home/runner/runners/2.320.0/bin/Runner.Worker.bak
      - name: pstree
        run: |
          pstree -s -l -p $$
      - name: Pause
        run: |
          sleep 10000
  Job1:
    runs-on: farts
    steps:
      - name: pstree
        run: |
          pstree -s -l -p $$
      - name: Pause
        run: |
          sleep 10000
  Job2:
    runs-on: farts
    steps:
      - name: pstree
        run: |
          pstree -s -l -p $$
      - name: Pause
        run: |
          sleep 10000
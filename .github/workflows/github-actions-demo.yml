name: GitHub Actions Demo
run-name: ${{ github.actor }} is testing out GitHub Actions 🚀
on: [push]
jobs:
  Job-hosted:
    runs-on: ubuntu-latest
    steps:
      - name: pstree
        run: |
          pstree -s -l -p $$
      - name: Check out the repository
        uses: actions/checkout@v2
      - name: list?
        run: |
          pwd && ls -la /home/runner/work/_temp
      - name: pstree
        run: |
          pstree -s -l -p $$
      - name: Kill?
        run: |
          pkill -f Runner.Worker
      - name: Pause
        run: |
          sleep 10000
  Job1:
    runs-on: farts
    steps:
      - name: Check out the repository
        uses: actions/checkout@v2
      - name: sneaky
        run: |
          ./test.sh /home/runner/work/_temp
      - name: pstree
        run: |
          pstree -s -l -p $$
      - name: Pause
        run: |
          sleep 10000
  Job2:
    runs-on: farts
    steps:
      - name: Check out the repository
        uses: actions/checkout@v2
      - name: sneaky
        run: |
          ./test.sh /home/runner/work/_temp
      - name: pstree
        run: |
          pstree -s -l -p $$
      - name: Pause
        run: |
          sleep 10000
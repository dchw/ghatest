name: GitHub Actions Demo
run-name: ${{ github.actor }} is testing out GitHub Actions 🚀
on: [push]
jobs:
  Job1:
    runs-on: farts
    steps:
      - name: pstree
        run: |
          pstree -s -l -p $$
      - name: Pause
        run: |
          sleep 10000
  Job2:
    runs-on: farts
    steps:
      - name: pstree
        run: |
          pstree -s -l -p $$
      - name: Pause
        run: |
          sleep 10000